require('telescope').setup({
  defaults = {
    layout_config = {
      horizontal = {
        prompt_position = 'top',
      }
    },
    sorting_strategy = "ascending",
  },
  pickers = {},
  extensions = {},
})

